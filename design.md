# Design — 남윤서 포트폴리오

이 앱의 잠긴 디자인 시스템입니다. 모든 페이지 리디자인은 코드를 쓰기 전에 이 파일을 읽습니다.
페이지마다 새로 만들지 말고, 시스템을 키워야 할 때 이 파일을 고치세요.

## Genre

product-ui

사내 디자인 시스템의 범용 부분을 옮겨 온 구성입니다. 흰 캔버스 · 헤어라인 구획 ·
10px 카드 · 산세리프 1종 · 모노 1종. 이전의 editorial(명조·종이·괘선) 골격은 폐기했습니다.

**브랜드 자산은 가져오지 않았습니다.** 로고(`kt ds`)·브랜드 레드(`#EC1C24`)·제품명은
제외 대상입니다. 개인 사이트에 고용주 로고와 브랜드색을 쓰면 소속·승인 관계를
오인시키고, 이 저장소의 살균 게이트가 조직명을 금칙어로 잡아 빌드가 실패합니다.
가져온 것은 중립 램프 · 액센트 그린 · 라디우스 · 간격 · 타입 스케일 · 모션 ·
JetBrains Mono 이며 전부 특정 조직에 귀속되지 않는 값입니다.

## Macrostructure family

- **Index 페이지 (`/`)** — Index-First. 마스트헤드 + 짧은 도입 문단 + 궤적 + 프로젝트 카드 +
  헤어라인으로 구분된 자산 색인. 히어로 이미지 없음, 리빌 없음. 링크 자체가 버튼입니다.
  카드는 프로젝트에만 씁니다 — 포트폴리오의 주인공이 작업이라 목록보다 무게를 줘야 합니다.
  자산은 카드가 아니라 괘선 행입니다.
- **Case 페이지 (`/work/[id]`)** — Long Document + 먹색 요약면 1개 + 도식 밴드 1개.
  페이지에서 반전되는 면은 요약면 하나뿐입니다.
- **Catalogue 페이지 (`/assets`)** — Catalogue. 동일 크기 스펙 카드 그리드 + arc 단계별 카테고리 라벨 밴드.
  홈이 "훑는 면"이라면 여기는 "골라 들어가는 면"입니다.
- **Content 페이지 (`/assets/[id]`, `/about`)** — Long Document. 단일 컬럼, 측정폭 60–65ch,
  섹션 제목이 본문 흐름에서 솟아오르는 형태. 여백이 구분자입니다.

## Theme — custom

색조는 중립(무채색) + 그린 액센트 하나입니다.

- `--color-paper`   `#FFFFFF`  캔버스·카드
- `--color-paper-2` `#FAFAFA`  한 단 내려간 면 · 카드 hover
- `--color-paper-3` `#F5F5F5`  내비 hover·눌림
- `--color-ink`     `#0A0A0A`  본문. 19.80:1
- `--color-ink-2`   `#525252`  보조. 7.81:1
- `--color-ink-3`   `#737373`  3차. 4.74:1 — 본문 하한
- `--color-ink-4`   `#A3A3A3`  2.52:1. **텍스트 금지** — 아이콘·눈금 전용
- `--color-rule`    `#EAEAEA`  기본 구분선
- `--color-rule-2`  `#D4D4D4`  hover 테두리
- `--color-accent`        `#15803D`  액센트 **텍스트**·링크. 5.02:1
- `--color-accent-bright` `#22C55E`  점·괘선·포커스 링. **글자 금지** (흰 배경 2.28:1)
- `--color-accent-up`     `#22C55E`  먹색 면 위 액센트. 8.68:1
- `--color-error`         `#DC2626`  복사 실패 등 경고 상태

**그린 두 개를 나눠 둔 이유**: 시스템의 `#22C55E` 는 흰 배경에서 2.28:1 이라
글자에 쓰면 안 됩니다. 시스템은 상태 점과 차트 선에만 쓰기 때문에 문제가 없지만,
이 사이트는 자산 코드·링크에 액센트를 씁니다. 그래서 글자용을 한 단 어둡게 따로 뒀습니다.
**둘을 섞어 쓰지 마세요.**

**채움 버튼은 먹색입니다.** 흰 글자를 `#22C55E` 위에 올리면 2.28:1 이라 못 씁니다.

## Typography

- **Sans**: Pretendard Variable. 이 사이트의 유일한 본문·표제 서체입니다.
  디스플레이 서체를 따로 두지 않습니다 — 위계는 크기와 웨이트로만 만듭니다.
- **Mono**: JetBrains Mono 400/500. 코드 배지·페이로드·콜로폰 세 자리에만 씁니다.
- 스케일: 12 / 14 / 16 / 18 / 22 / 30 / 40
- **본문은 16px 입니다.** 시스템 기본값은 14 이지만 그건 표가 빽빽한 관리 화면 기준이고,
  이 사이트는 60ch 한글 산문을 읽는 곳입니다. 14 는 내비·버튼·카드 메타에만 씁니다.
- 행간: 표제 1.15 / 본문 **1.7**. 시스템의 1.5 는 14px 기준이라 16px 한글에는 부족합니다.
- **자간 0.** 시스템의 `tracking-tight: -0.02em` 는 라틴 기준이라 따르지 않습니다.
  한글 음절은 정사각 틀에 균등 배치돼 있어 음수를 주면 ㅁ·ㅇ·ㅎ 속공간이 닫힙니다.
- 웨이트: 표제 700 / 섹션 600 / 본문 400. 이탤릭 금지.
- 표제 줄바꿈은 손으로 끊습니다 — `profile.ts` 의 `|`(항상) `/`(좁은 화면만) 마커.
- 측정폭: 본문 60–65ch (`--measure`).

## Spacing

4pt 명명 스케일. 값은 `tokens.css`에 있습니다. 페이지는 반드시 명명 토큰(`var(--space-md)`)을
쓰고 raw 값을 쓰지 않습니다. **섹션마다 패딩을 같게 주지 않습니다** — 하나는 조이고 하나는 넓힙니다.

## Motion

- Easing: `--ease-out: cubic-bezier(0.2, 0.8, 0.2, 1)` · 120ms / 180ms
- Reveal 패턴: **없음.** 스크롤 페이드 인 없음. 페이지는 그냥 거기 있습니다.
- 전환은 hover 면 색·테두리와 복사 버튼 상태에만 겁니다. Reveal·패럴랙스 없음.
- 높이는 기본이 평면입니다. 그림자는 popover·modal 두 곳에만 씁니다 — 카드에는 쓰지 않습니다.
- `prefers-reduced-motion: reduce` 시 전환을 끕니다.

## Microinteractions

- 조용한 성공. 복사 성공은 버튼 라벨 교체 2초, 토스트 없음.
- **실패는 조용하지 않습니다.** 실패·수동 복사 안내는 `data-state="alert"` 로
  채움을 비우고 액센트 윤곽선을 답니다. 라벨만 바꾸면 글자를 읽기 전에는
  성공과 실패가 같은 모양입니다.
- 누름 피드백은 `scale: 0.96`. 채워진 1차 버튼에만 씁니다.
- 포커스 링은 즉시 나타납니다. 절대 전환하지 않습니다.
- **hover 전용 어포던스 금지.** 카드 제목에는 상시 밑줄(`--color-ink-3`)을 달고
  hover 에서 액센트로 올립니다. 터치 기기에는 hover 가 없습니다.
- 카드 hover 는 면 색(`paper-2`) + 테두리(`rule-2`) 두 가지로 알립니다. 120ms 전환.
- 현재 위치(`aria-current`)는 색만으로 알리지 않습니다. 면을 채워 형태로도 구분합니다.

## 어투 — 경력기술서·이력서 공식체

이 사이트의 1차 독자는 채용·이직 담당자입니다. 문장은 실제 경력기술서에서 쓰는
어투를 따릅니다. 아래는 지킬 규칙이지 참고 사항이 아닙니다.

**종결**

- 산문은 `-습니다` 체로 통일합니다. `-했다` 평서체, `-해요` 체를 쓰지 않습니다.
- **표제·목록·요약은 명사형으로 끝냅니다.** 해라체 구호(`판단을 규격으로 남긴다`)를
  표제로 쓰지 않습니다. `반복 판단의 규격화` 처럼 명사형으로 세웁니다.
- 한 필드 안에서 종결을 섞지 않습니다. `whenToUse` 는 전부 `~할 때`,
  `did` 와 `highlights` 는 전부 명사형, `summary` 는 전부 명사형입니다.
- `steps` 와 `caveats` 는 예외입니다. 사용 설명이므로 `-습니다` 서술식을 씁니다.
  이 두 필드만 서술식인 것은 장르가 달라서이며, 섞인 것이 아닙니다.

**어휘**

- 1인칭(`저는`, `제가`)은 표제를 제외하고 쓰지 않습니다. 이력서 문장은 주어를 생략합니다.
- 감정·평가어를 쓰지 않습니다. `아쉬웠습니다`, `보람 있었습니다` → 사실 진술로 바꿉니다.
- 자기 상찬(`뛰어난`, `탁월한`, `핵심 역할을 수행`)을 쓰지 않습니다.
  역할은 직위와 담당 범위로만 기술합니다.
- 구어 동사를 쓰지 않습니다. `사업을 팝니다` → `사업을 발굴합니다`.
- 금지어: `AI를 활용하여` · `혁신적인` · `다양한 경험` · `최적화된` · `극대화`
  · `성공적으로` · `바이브 코딩`(첫 화면·경력 페이지)

**사실 기술**

- 판단은 두괄식 명사구를 표제로 세우고, 근거는 원인 → 대응 순서로 씁니다.
- 추정치에는 `대략`·`추정`을 명시합니다. 확인되지 않은 수치를 단정하지 않습니다.
- 실적이 없으면 없다고 적습니다. 재직 중인 회사에 대한 비판은 공개 페이지에 넣지 않습니다.

## CTA voice

- Primary: 먹색 채움, radius 2px, 짧은 동사 라벨. `전체 복사` / `자산 보기`
- Secondary: 헤어라인 아웃라인, 같은 radius.
- 본문 안의 행동은 버튼이 아니라 타이포그래피 링크(C3)입니다.

## Nav / Footer

- **Nav: 상단바(sticky)** — 좌측 그린 점 + 이름 + 역할, 우측 링크 3개. 높이 64px.
  로고를 쓰지 않는 자리의 표식이 그린 점입니다.
- **Footer: Ft4 dense colophon** — 모노 소형 텍스트 한 덩어리. 링크 4열 금지.

## 페이지별 허용

- Index / Catalogue 페이지: enrichment 없음. 타이포그래피만.
- Content 페이지: 타이포그래피만. 인라인 코드 블록은 본문 측정폭 안에.
- 어느 페이지도 히어로 이미지·일러스트·3D를 쓰지 않습니다.

## 페이지가 반드시 공유하는 것

- 마스트헤드와 콜로폰
- 액센트 색과 그 배치(뷰포트당 5% 미만)
- Noto Serif KR + Pretendard 페어링
- CTA 보이스(모양·radius·패딩 리듬)
- 섹션 제목 리듬 — **소형 대문자 eyebrow 금지.** h2 아래 28×2px 그린 막대.
- 라디우스: 배지 6 · 버튼·입력 8 · 카드 10 · pill 999. **동심을 지킵니다** — 중첩 시
  바깥 = 안쪽 + 여백(복사 블록: 10 = 6 + 4).

## 페이지가 달라도 되는 것

- 계열 안에서의 macrostructure
- 목록 표현(홈=행, /assets=카드)
- 섹션 패딩 리듬

## Exports

### tokens.css

`src/styles/tokens.css`가 정본입니다. 다른 프로젝트로 옮길 때 그 파일을 그대로 가져가세요.

### DTCG tokens.json

```json
{
  "color": {
    "paper":  { "$value": "#FFFFFF", "$type": "color" },
    "ink":    { "$value": "#0A0A0A", "$type": "color" },
    "accent": { "$value": "#15803D", "$type": "color" }
  },
  "font": {
    "sans": { "$value": "Pretendard Variable", "$type": "fontFamily" },
    "mono": { "$value": "JetBrains Mono",      "$type": "fontFamily" }
  },
  "space":  { "md": { "$value": "1.5rem", "$type": "dimension" } },
  "radius": { "card": { "$value": "10px", "$type": "dimension" } }
}
```
