# 직접 고치는 법

사이트: **https://namtank96.github.io** (로그인 없이 누구나 열림)
편집기: **https://namtank96.github.io/admin/**

---

## 처음 한 번만 — 로그인

1. `https://namtank96.github.io/admin/` 을 엽니다.
2. **"Sign in with Token"** 을 누릅니다.
3. 화면에 뜨는 링크를 누르면 GitHub 토큰 발급 페이지가 **필요한 권한이 미리 체크된 상태로** 열립니다.
   그대로 발급받으세요.
4. 발급된 토큰을 편집기 입력칸에 붙여넣습니다.

토큰은 **이 브라우저에만** 저장됩니다. 서버로 가지 않고, 이 저장소에도 들어가지 않습니다.
다른 기기(폰 포함)에서 쓰려면 그 기기에서 같은 절차를 한 번 더 하면 됩니다.

> 토큰 만료 기간을 물어보면 90일 정도로 잡으세요. 만료되면 다시 발급받아 넣으면 됩니다.

---

## 무엇을 고칠 수 있나

편집기 왼쪽에 네 묶음이 있습니다.

| 묶음 | 무엇 | 새로 만들기 |
|---|---|---|
| **프로젝트** | 어디서 무슨 일을 했고 무엇을 판단했는지 | 가능 |
| **경력 (회사)** | 회사 단위 정보 | 불가 (아래 참고) |
| **자산 (설명)** | 자산 페이지의 설명 부분 | 가능 |
| **자산 (복사 본문)** | 복사 버튼으로 나가는 원문 | 불가 (아래 참고) |

고치고 **Publish** 를 누르면 커밋이 올라가고, **약 90초 뒤 사이트에 반영**됩니다.

---

## 지금 비어 있는 칸 — 여기부터 채우면 좋습니다

**프로젝트 → 학습 콘텐츠 서비스 0→1**, **프로젝트 → LMS 서비스 0→1**
두 건의 **판단** 칸이 비어 있습니다. 그 시절 산출물이 남아 있지 않아 재구성할 수 없었습니다.

`판단` 항목을 추가하고 두 칸을 채우면 됩니다.

- **무엇을 정했나** — 그때 내린 결정 한 줄
- **왜 그렇게 정했나** — 그 근거. 지금 보니 틀린 판단이었다면 그렇게 적어도 됩니다

예시:

> **무엇을 정했나** — 결제 단계를 줄이는 대신 이탈 지점을 분리해서 봤다
> **왜 그렇게 정했나** — 단계를 합치면 한 화면에 입력이 몰려 오히려 이탈이 커진다고 봤다.
> 실제로 어느 단계에서 빠지는지부터 나눠 보고 나서 손대기로 했다.

지금 이전 경력 2건은 **사실 나열까지만**이고, 현직 3건은 판단까지 들어가 있습니다.
이 두 칸이 채워지면 경력 전체가 같은 깊이가 됩니다.

---

## 지켜야 할 것 세 가지

### 1. 고객사명·사내 과제명·플랫폼명을 쓰지 않습니다

식별 수준은 **업종과 규모까지**입니다. "금융권 대기업", "약 1년", "12인 규모" 는 괜찮고,
회사 이름·제품 코드명·사내 시스템명은 안 됩니다.

실수로 쓰더라도 **배포 전 자동 검사가 막습니다.** 사이트는 마지막 정상본을 유지하고,
GitHub 의 Actions 탭에 무엇이 걸렸는지 표시됩니다.

### 2. 없는 숫자를 만들지 않습니다

확인되지 않은 성과·수치는 비워두세요. 비어 있으면 그 칸이 렌더되지 않을 뿐입니다.
지어낸 숫자 하나가 나머지 전부의 신뢰를 깎습니다.

### 3. 복사 본문은 200줄을 넘기지 않습니다

넘으면 자산을 둘로 쪼개세요. 그리고 복사 본문에는 **경로·회사명·사람 이름을 넣지 않습니다** —
그대로 남의 클립보드로 갑니다.

---

## 새 자산을 만들 때

편집기에서 **자산 (설명)** 은 새로 만들 수 있지만, 짝이 되는 **복사 본문** 파일은
편집기에서 못 만듭니다. 순서가 이렇습니다.

1. GitHub 에서 `src/payloads/` 폴더로 갑니다 → **Add file → Create new file**
2. 파일명을 `a16-something.md` 처럼 **영문 소문자·숫자·하이픈**으로 짓습니다
3. 복사될 원문을 적고 커밋합니다
4. 편집기로 돌아와 **자산 (설명)** 을 새로 만들면서, **복사 본문 파일** 칸에
   `a16-something` (확장자 없이) 을 적습니다

한글 파일명은 쓰지 마세요. 내 PC 와 배포 서버가 한글을 다르게 저장해서,
**로컬에서는 되는데 실제 사이트에서만 404** 가 나는 문제가 생깁니다. 눈으로는 구분이 안 됩니다.

---

## 뭔가 잘못됐을 때

**사이트가 안 바뀝니다**
GitHub 저장소 → **Actions** 탭. 빨간 X 가 있으면 눌러서 이유를 봅니다.
대개 살균 검사(금칙어)나 형식 오류입니다. **사이트는 계속 마지막 정상본을 보여줍니다.**

**되돌리고 싶습니다**
저장소 → **Commits** → 되돌릴 커밋 → **Revert**. 90초 뒤 원래대로 돌아갑니다.

**편집기가 안 열립니다**
토큰이 만료됐을 수 있습니다. 편집기에서 로그아웃하고 새 토큰으로 다시 넣으세요.

---

## 편집기 말고 다른 방법

- **GitHub 웹에서 직접** — 저장소에서 파일을 열고 연필 아이콘. 저장소 화면에서 `.` 키를 누르면
  브라우저 안에 VS Code 가 뜹니다. 설치할 것 없습니다.
- **내 PC 에서** — `npm run dev` 로 즉시 미리보기. 단 Node 24 가 PATH 앞에 와야 합니다
  (지금은 구버전 20 이 가리고 있습니다).
