/**
 * 금칙어 사전.
 *
 * 이 사이트의 자산은 전부 사내 산출물을 일반화한 것입니다.
 * public repo 는 되돌릴 수 없으므로(파일을 지워도 git history 에 남고,
 * force-push 해도 GitHub 캐시·포크·크롤러는 못 지웁니다),
 * "사람이 기억해서 지키는" 방식 대신 빌드 파이프라인에 게이트를 둡니다.
 *
 * 판정 원칙: 형태는 공개, 내용은 비공개.
 *   → "이 문장을 경쟁사 사업개발 담당자가 읽으면 우리 회사에 대해
 *      새로 알게 되는가?" 예면 삭제.
 *
 * 이 파일 자체는 검사 대상에서 제외됩니다(자기 자신을 잡으면 안 되므로).
 */

/** @type {{ id: string, re: RegExp, why: string }[]} */
export const FORBIDDEN = [
  // ── 조직·소속 ───────────────────────────────────────
  {
    id: 'org',
    re: /kt\s?ds|KTDS|\bKT\b|AX컨설팅팀/gi,
    why: '조직·소속 식별자',
  },

  // ── 제품·프로젝트 코드명 ────────────────────────────
  {
    id: 'codename',
    re: /\bAAP\b|Agentic\s?AI\s?Platform|MAESTRO|\bBEAST\b|Antbot|AI:?ON-?U|명장GPT|PoC\s?Maker/gi,
    why: '사내 제품·프로젝트 코드명',
  },

  // ── 내부 경로·파일명 체계 ───────────────────────────
  {
    id: 'path',
    re: /OneDrive|C:\\Users|\bnamta\b|_범용플랫폼|_기획안|_핸드오프|_선제안서|_사업화전략|v0_\d/g,
    why: '내부 경로·폴더 번호체계·파일명 패턴',
  },

  // ── 도메인 룰 값 (고객사 업무규칙이 드러남) ──────────
  {
    id: 'domain-rule',
    re: /손해사정|여신한도|\bDSR\b|\bLTV\b|신용컷|렌탈료|보조금\s?자격/gi,
    why: '고객 도메인 룰 값 — 어느 업종 프로젝트였는지가 드러남',
  },

  // ── 사업 수치 ───────────────────────────────────────
  {
    id: 'figure',
    re: /\d+\s?억(?!\s?년)|MAU\s?\d|전환율\s?\d|월\s?매출\s?\d/g,
    why: '사업 수치(가격·매출·전환율) — 경력 페이지 성과 표기는 예외 처리 필요',
  },

  // ── 인증·조달 전략 ──────────────────────────────────
  {
    id: 'strategy',
    re: /CSAP|GS인증|조달청\s?등록|혁신제품\s?지정/gi,
    why: '사업 전략 판단 — 방법론으로도 전환 금지 대상',
  },
];

/** 검사할 경로 (콘텐츠와 사용자 노출 텍스트만). */
export const SCAN_GLOBS = [
  'src/**/*.md',
  'src/**/*.astro',
  'src/**/*.ts',
  'src/**/*.css',
  'public/**/*.txt',
  'README.md',
];

/** 검사에서 제외할 경로. */
export const IGNORE_GLOBS = [
  'scripts/**', // 사전 자체
  'node_modules/**',
  'dist/**',
  '.astro/**',
];
