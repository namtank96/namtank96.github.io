# 모든 텍스트 파일을 LF로 정규화합니다.
#
# 이 설정이 없으면 Windows 에디터가 쓴 CRLF가 그대로 커밋되고,
# src/payloads/*.md 의 원문이 <pre>를 거쳐 클립보드로 들어갈 때
# CRLF가 딸려가 Linux 터미널·YAML 블록에서 깨집니다.
#
# 주의: 이 규칙은 적용 이후 추가되는 파일에만 유효합니다.
* text=auto eol=lf
